{
  "name": "electron-uv-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp rebuild && electron-rebuild"
  },
  "author": "",
  "license": "ISC",
  "gypfile": true,
  "devDependencies": {
    "electron": "^9.0.5",
    "electron-rebuild": "^1.11.0",
    "node-addon-api": "^3.0.0"
  }
}
